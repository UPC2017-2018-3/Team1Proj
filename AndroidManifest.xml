<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.canyin"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.example.canyin.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.canyin.second"
            android:label="管理界面" >
            </activity>
             <activity
            android:name="com.example.canyin.caidan"
            android:label="菜单管理" >
            </activity>
            <activity
            android:name="com.example.canyin.yonghugl"
            android:label="用户管理" >
            </activity>
             <activity
             android:name="com.example.canyin.zhuceyemian"
            android:label="用户注册" >
            </activity>
    </application>

</manifest>
